# Hermes plugin manifest (YAML).
# Spec: https://hermes-agent.nousresearch.com/docs/developer-guide/plugins
# Loaded by `hermes plugins` from ~/.hermes/plugins/<name>/plugin.yaml
name: obscura
version: "0.2.0"
description: "Headless browser as tools — fetch web pages, scrape URLs, extract content via obscura CLI."
author:
  name: epicsagas
  url: https://github.com/epicsagas
provides_tools:
  - obscura_fetch_page
  - obscura_scrape_urls
  - obscura_extract_markdown
