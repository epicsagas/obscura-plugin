{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}/hooks/install.js\"",
            "async": false,
            "statusMessage": "Setting up Obscura — installing binaries and registering MCP server"
          }
        ],
        "description": "Install Obscura binaries if missing, register MCP server and seed skills/agents"
      }
    ]
  }
}
