{
  "name": "obscura-plugin",
  "owner": {
    "name": "epicsagas",
    "url": "https://github.com/epicsagas"
  },
  "metadata": {
    "description": "Rust headless browser as MCP tools. Fetch, scrape, extract markdown — zero config, auto-installs."
  },
  "plugins": [
    {
      "name": "obscura",
      "source": "./",
      "description": "Rust headless browser as MCP tools. Fetch, scrape, extract markdown — zero config, auto-installs."
    }
  ]
}
